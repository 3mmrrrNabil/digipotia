enum ReportCategory { Security, PublicSafety, Traffic, Environment, Other }

enum ReportVisibility { Public, Confidential, Anonymous }

enum ReportStatus { Pending, InReview, Dispatched, Resolved, Rejected }

enum TrustBadge { Newcomer, Contributor, Trusted, Guardian, Vanguard }

enum UserRole { User, Admin, Authority }

